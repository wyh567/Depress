# T-xx — <任务标题>

- **状态**：`NOT_STARTED`
- **前置任务**：`T-xx` / 无
- **预计改动文件数**：N
- **是否需要用户批准才能开工**：是 / 否

## 为什么做这个

<一段话。说明不做会怎样，做了解决什么。不要写行动步骤。>

## 前置条件（逐条验证，任一不满足就停）

- [ ] `<可执行命令>` → 期望：`<期望输出>`
- [ ] `03-task-board.md` 中前置任务状态为 `DONE`

## 允许触碰的文件（白名单，超出即为越界）

- `path/to/file`

## 禁止触碰的文件

- `path/to/file` —— 原因

## 执行步骤

> 严格按顺序。每步做完先确认，再做下一步。

1. …
2. …

## 验收标准

> **每一条都必须真的跑命令**，不许凭感觉判断。全过才算 `DONE`。

| # | 检查 | 命令 | 期望 |
|---|---|---|---|
| 1 | | | |

## 停止条件

- <什么情况下必须停下报告用户>

## 完成后必须更新

- [ ] `03-task-board.md` 状态改 `DONE` + 日期
- [ ] `01-current-state.md` 对应事实
- [ ] 本文件 `## 完成记录`

## 完成记录

> 由执行 agent 填写。写实测结果，不写计划。

- 完成日期：
- 实际改动文件：
- 验收实测输出：

## 阻塞记录

> 卡住时填。写清楚：第几步、跑了什么命令、实际输出、你判断的原因。

-
